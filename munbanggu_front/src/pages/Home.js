import React from "react";
import CardList from "../components/CardList";

const Home = (props) => {
    return (
        <React.Fragment>
            <CardList />
        </React.Fragment>
    );
};

export default Home;
