import React from "react";
import CardList from "../components/CardList";
import Card from "../components/Card";

const Home = (props) => {
    return (
        <React.Fragment>
            <CardList />
        </React.Fragment>
    );
};

export default Home;
